# 🗺️ Roadmap & Changelog — Pokémon: Scegli il Cammino

Questo documento traccia l'evoluzione del progetto, le funzionalità implementate versione per versione e le idee pianificate per il futuro.

---

## 📜 Storico delle Versioni (Funzionalità Completate)

### Versione 1.0 — Prova di Concetto (v1)
- [x] Starter unico della prima generazione (Gen 1).
- [x] Struttura base: bivio iniziale, incontro selvatico, singola palestra, sfida col rivale e schermata finale.
- [x] Calcolo esito basato sulle scelte narrative e sulla potenza complessiva della squadra (senza ruota della fortuna).
- [x] Integrazione con PokeAPI per sprite e dati dei Pokémon in tempo reale.

---

### Versione 2.0 — Regioni e Campionato Completo (v2)
- [x] **Selezione della Regione**: supporto per Kanto (Gen 1) e Johto (Gen 2).
- [x] **Campionato completo**: 8 Palestre fedeli per ordine e tipo, seguiti dall'Alto Comando (4 membri) e il Campione della Lega.
- [x] **Tappe di Esplorazione**: bivi esplorativi scalati in 3 tier di difficoltà tra una palestra e l'altra (erba alta, pesca, grotta con oggetti, o allenamento).
- [x] **Sfida Rivale**: evento battaglia a sorpresa a metà percorso.
- [x] **Barra di Avanzamento**: indicatore visivo del progresso nella Lega.
- [x] **Script di simulazione**: `scripts/simulate-flow.mjs` per validare la sequenza di gioco a secco.

---

### Versione 5.0 — Sala della Fama & Hall of Fame Storica (v5)
- [x] Persistenza automatica in `localStorage` (`pcq_hall_of_fame`) ad ogni vittoria contro il Campione della Lega in qualsiasi regione.
- [x] Registro d'onore permanente con nome della regione, data e ora del trionfo, membri della squadra vincente e badge Nuzlocke.
- [x] **📖 Modale `HallOfFameModal.js`**: tema dorato stile champagne con carte celebrate per ogni trionfo e sprite visivi pixel di tutti i Pokémon campioni.
- [x] **🔘 Pulsante Header (`🏆 Sala della Fama`)**: accesso rapido consultabile in qualsiasi momento nell'header della app accanto al Pokédex.

---

### Versione 6.5 — Torneo dei Campioni della Lega Post-Game (v6.5)
- [x] **Sblocco speciale del Torneo**: nel Post-Game è possibile sfidare i 5 Campioni leggendari in un duello ad eliminazione diretta.
- [x] **Round 1**: 🔴 *Campione Rosso (Kanto)* — Pikachu, Charizard, Blastoise, Venusaur (Potenza 180).
- [x] **Round 2**: 🪨 *Campione Rocco Petri (Hoenn)* — Metagross, Skarmory, Aggron, Armaldo (Potenza 195).
- [x] **Round 3**: 🌸 *Campionessa Camilla (Sinnoh)* — Garchomp, Lucario, Milotic, Spiritomb (Potenza 215).
- [x] **Round 4**: 🐉 *Campione Imbattibile Dandel (Galar)* — Charizard, Dragapult, Aegislash (Potenza 235).
- [x] **Grand Finale**: 👑 *Prima Campionessa Alisma (Paldea)* — Glimmora, Kingambit, Espathra (Potenza 260).
- [x] **🏆 Tabellone del Torneo (`TournamentScene.js`)**: schermata del tabellone ad eliminazione diretta con stato per ogni sfida.
- [x] **👑 Iscrizione nella Sala della Fama**: titolo supremo `👑 RE DEI CAMPIONI POKÉMON` e boost +5 livelli per l'intera squadra dopo la vittoria finale.

---

### Versione 7.0 — Sistema Abilità Passive dei Pokémon (v7.0)
- [x] Modulo `src/data/abilities.js` per l'assegnazione e il calcolo delle abilità passive delle varie specie.
- [x] **🦁 Prepotenza (Intimidate)**: riduce la potenza dell'avversario del **-10%** ad ogni scontro.
- [x] **⚡ Pressione (Pressure)**: aumenta la probabilità di successo in battaglia del **+8%**.
- [x] **🛡️ Levitazione (Levitate)**: annulla le penalità di svantaggio di tipo (-10% -> 0%).
- [x] **🌊 Nuotavelox (Swift Swim)**: +15% di potenza nelle zone d'acqua e pesca.
- [x] **💥 Acceleratore (Speed Boost)**: +5% alla probabilità di successo tattico in battaglia.
- [x] **🍀 Leggiadria (Serene Grace)**: +10% alla probabilità di cattura Pokémon selvatici.
- [x] **✨ Visualizzazione UI**: display dei badge delle abilità attive in `TeamPanel.js` ed in `BattleScene.js`.

---

### Versione 7.5 — Centro Pokémon & Mercatino PokéMart (v7.5)
- [x] Nuova tappa esplorativa `🏥 Centro Pokémon & Mercatino di Città` presente nelle tappe casuali tra le palestre.
- [x] **👩‍⚕️ Infermeria Joy**: ripristina la salute della squadra con jingle audio retro Game Boy a 8-bit (`playHealJingle()`).
- [x] **💰 Sistema Pokédollari**: moneta di gioco guadagnata vincendo sfide contro Capipalestra (+5 💰), Boss dei Team nemici (+4 💰) ed Allenatori (+2 💰).
- [x] **🏪 Mercatino PokéMart**: acquisto di Pozioni (1 💰), Super Pozioni (2 💰), Iper Pozioni (3 💰), Caramelle Rare (5 💰) e Master Ball (10 💰).
- [x] **🏷️ Display Pokédollari**: indicatore del saldo Pokédollari integrato nel pannello laterale della squadra (`TeamPanel.js`).

---

### Versione 8.0 — Classifica Punteggio & Grado di Vittoria (v8.0)
- [x] Modulo `src/engine/scoreLogic.js` per il calcolo matematico del punteggio di vittoria e del grado d'onore.
- [x] **🏆 Grado S — Maestro Pokémon Supremo** (>= 25.000 pt)
- [x] **🥇 Grado A — Allenatore d'Élite** (15.000 - 24.999 pt)
- [x] **🥈 Grado B — Veterano della Lega** (8.000 - 14.999 pt)
- [x] **🥉 Grado C — Allenatore Promettente** (< 8.000 pt)
- [x] **📊 Componente `ScoreCardModal.js`**: scheda d'onore celebrativa con trofeo visivo animato, riepilogo dettagliato punti (Medaglie, Livelli Squadra, Specie Pokédex, Bonus Shiny e Moltiplicatore Nuzlocke x1.5) e record personale salvato in `localStorage`.
- [x] **🔘 Pulsante Header (`📊 Punteggio`)**: accesso rapido consultabile in qualsiasi momento nell'header della app.

---

### Versione 8.5 — Refactoring Ultra-Modulare & Sanitizzatore (v8.5)
- [x] **🧹 Refactoring Architetturale**: scomposizione di `App.js` ed `useGameState.js` in sotto-hook compatti (`useSaveSlot.js`, `usePokedexState.js`) e sotto-container per le scene (`ExploreSceneContainer.js`, `GymBattleSceneContainer.js`, `LeagueSceneContainer.js`).
- [x] **🛡️ Sanitizzatore Automatico dei Salvataggi (`saveSanitizer.js`)**: validatore automatico per ripristinare valori sicuri in caso di salvataggi parziali o corrotti in LocalStorage.
- [x] **🧪 Suite di Test Automatici (`npm test`)**: test runner nativo di Node.js (`node --test`) con 7 test unitari su logica di battaglia, punteggio, tipo e salvataggi.
- [x] **🔍 Logger Diagnostico (`logger.js`)**: tracciamento pulito delle transizioni di fase ed esiti battaglie in console DevTools.

---

### Versione 8.6 — Randomizer/Mono-Type, Bilanciamento & Strumenti di Analisi Run (v8.6 - Attuale)
- [x] **🎲 Randomizer Mode & 🔥 Mono-Type Challenge riattivati**: toggle nella schermata `GenerationSelectScreen`; il filtro (`filterEncounterPoolByChallenge`) viene ora applicato centralmente in `useGameState.goTo()` al momento della transizione di stato, non nel render — fix definitivo del bug storico di re-render infinito (vedi sezione TODOLIST/Fix qui sotto, ora risolta).
- [x] **🐛 Fix incontri leggendari**: il filtro Randomizer/Mono-Type non viene più applicato ai pool di incontro leggendario (che contengono un solo id) — prima poteva sostituire il leggendario con un Pokémon comune qualsiasi dello stesso tipo.
- [x] **⚔️ Mega Evoluzione e Terastallizzazione mutuamente esclusive**: in battaglia non è più possibile attivare entrambe nella stessa battaglia (prima cumulabili e gratuite, +62.5% di potenza combinata senza alcun costo).
- [x] **🐛 Fix bivio di esplorazione ripetuto identico**: il seed che sceglie le opzioni speciali del bivio principale dipendeva solo da `gymIndex`/`badges.length` (fissi per l'intera durata di una palestra) — un'opzione poteva dominare per intere run mentre altre non comparivano mai. Ora il seed incorpora `choicesCount` (che avanza ad ogni transizione di stato) e l'hash di mescolamento è stato sostituito con FNV-1a + un finalizzatore a 32 bit (prima l'ordinamento guardava solo la prima lettera dell'id).
- [x] **📐 Bivi allargati da 3 a 6 opzioni speciali** mostrate per bivio principale (oltre a erba/allenamento/ruota del destino).
- [x] **⚖️ Bilanciamento incontro & cattura leggendari**: probabilità di incontro ridotta (peso 0.10 → 0.06, il più basso della tabella), tasso di cattura base abbassato (Poké Ball 15%→10%, Cibo 18%→12%), bonus Leggiadria dimezzato sui leggendari, tetto massimo 30%→20%. Master Ball resta garanzia 100% (invariato).
- [x] **🧪 Nuovo modulo puro `src/engine/explorePicker.js`**: logica di selezione dei bivi estratta da `ExploreSceneContainer.js` in un modulo senza React/`Math.random()`, testabile in isolamento. Tabella pesi condivisa in `src/data/exploreOptions.js` (unica fonte di verità tra componente e test).
- [x] **🧪 Nuova suite `tests/explorePicker.test.js`**: simula run intere (fino a 200×120 bivi) per verificare copertura totale delle opzioni, assenza di dominazione, rarità del leggendario, purezza/determinismo dell'algoritmo (incluso un test che fa esplodere `Math.random()` se il picker lo chiama mai) e cattura leggendari via Monte Carlo su 20.000 tentativi.
- [x] **📊 Registratore di sessione locale (`src/engine/runRecorder.js`)**: traccia bivi mostrati/scelti, incontri, catture e battaglie di una run reale. Autosave automatico ogni 10 eventi e alla chiusura/cambio scheda (`sendBeacon`), oltre a `pcqRunLog.save()`/`.download()` manuali da console DevTools.
- [x] **🖥️ Nuovo `server.mjs`**: server locale a zero dipendenze npm che sostituisce `npx serve .` come `npm start` — serve i file statici e accetta `POST /api/log` per scrivere i log di sessione in `logs/` (cartella esclusa da git).
  - 🐛 **Fix**: `npm start` poteva incappare in `EADDRINUSE: address already in use 127.0.0.1:3000` non gestito (crash immediato) se un processo `node` precedente non veniva chiuso e restava aggrappato alla porta — capitato più volte con processi server avviati per verifiche Playwright durante lo sviluppo, specie perché su questo setup Windows/Git Bash `kill %N` su un job in background non termina in modo affidabile un `node.exe` nativo (va usato `Stop-Process`/`taskkill` sul PID reale). Risolto rendendo `server.mjs` resiliente: alla `EADDRINUSE` prova automaticamente le porte successive (`3001`, `3002`, ... fino a 10 tentativi) invece di far crashare tutto, stampando chiaramente su quale porta è effettivamente partito. Nell'implementazione, un bug intermedio è emerso in verifica manuale: riusando lo stesso `Server` su più tentativi, il listener `"listening"` di un tentativo fallito (che non lo emette mai, solo `"error"`) restava agganciato anche al tentativo successivo — `.once` non basta a rimuoverlo da solo, va ripulito esplicitamente con `removeAllListeners` prima di ogni retry, altrimenti al bind riuscito scattano insieme sia il listener vecchio (con la porta sbagliata nel closure) sia quello nuovo, stampando due porte diverse come se fossero entrambe attive.
- [x] **📈 Nuovo `scripts/analyze-run-log.mjs` (`npm run logs`)**: legge i log reali in `logs/` e stampa un report con verdetto ⚠️/✅ automatico (dominazione bivi, ripetizioni identiche, scarto osservato/teorico su catture e battaglie), con soglie coerenti con `tests/explorePicker.test.js`.
- [x] **🐛 Fix icone medaglie/avatar avversario incoerenti**: `TeamPanel.js` e `BattleScene.js` indovinavano l'icona per string-matching sul nome della medaglia/titolo, in modo incoerente tra loro e fragile verso nomi nuovi — verificato che badge come "Medaglia Erba", "Medaglia Fuoco", "Medaglia Normale Paldea", "Medaglia Buio", "Medaglia Folletto", "Medaglia Lotta" e "Medaglia Ombra" non matchavano **nessuna** parola chiave e cadevano sull'icona generica 🏅. Sostituito con una mappa tipo→icona condivisa (`getTypeIcon` in `data/types.js`) risolta tramite il tipo reale del capopalestra (`getBadgeType` in `data/generations.js`, che lo ricava dal titolo "di tipo X", non dal nome estetico della medaglia). Spettro ha ora un'icona propria (👻) invece di condividere quella di Psico (🔮).
- [x] **⚖️ Iper Pozione ora più forte della Super Pozione**: +18 → +24 Potenza in battaglia (prima identica alla Super Pozione pur costando 3💰 contro 2💰).
- [x] **📝 Documentato il fallback euristico delle abilità passive** (`data/abilities.js`): commento esplicito che l'assegnazione per modulo dell'id (es. Weedle → Acceleratore) è riempimento arbitrario, non dati ufficiali dei giochi.
- [x] **🐛 Fix crash "Cannot read properties of null (reading 'starterIds')" al cambio regione**: bug preesistente (non introdotto in questa versione) segnalato da un giocatore reale. `nextGenId` (impostato al passaggio tra una Lega e la successiva) non era incluso nel whitelist di `saveSanitizer.js`: se si resta sulla schermata "nuova regione" e il gioco si ricarica/riprende (refresh, o Home → Riprendi), il campo va perso e cliccando "Esplora" `generationId` diventa `undefined`, mandando in crash `SceneRouter`. Aggiunto `nextGenId` a `initialState()` e al sanitizzatore; aggiunto anche un guardrail in `LeagueSceneContainer.js` che torna alla schermata Salvataggi invece di andare in crash se `nextGenId` risultasse comunque non valido. Riprodotto e verificato risolto iniettando un salvataggio nello stato esatto del bug report.
- [x] **🎨 Consolidati i colori CSS duplicati** (`BattleScene.js`, `TeamPanel.js`, `PokeCenterScene.js`, `HallOfFameModal.js`, `ScoreCardModal.js`, `NuzlockeGameOverScreen.js`): 16 esadecimali usati ripetutamente con valori leggermente diversi tra loro (es. più "rossi pericolo" diversi) sostituiti con le custom property esistenti (`--danger`, `--success`, `--text-dim`, `--text`) o nuovi token semantici condivisi (`--gold`, `--gold-light`, `--mega`, `--mega-light`, `--tera`, `--tera-light`, `--danger-dark`, `--panel-darker`). Nessun colore duplicato rimasto, nessuna differenza visiva (verificato in browser).
- [x] **📐 Sistema di 3 livelli di enfasi visiva riutilizzabili**: nuove classi CSS `.badge-status` (stato permanente: Nuzlocke/Randomizer/Mono-Tipo/Mega-Tera attivi), `.control-action` (pulsanti Mega/Tera) e `.info-chip` (abilità attive) al posto di uno stile inline nuovo per ogni feature — stessa resa visiva di prima, struttura condivisa.
- [x] **❓ Modale di onboarding al primo avvio** (`OnboardingModal.js`): 4 pannelli che spiegano il loop centrale (bivi, cattura, tattiche di battaglia, Mega/Tera/Box), mostrato una sola volta (flag in `localStorage`, indipendente dagli slot di salvataggio) e riapribile in qualsiasi momento dal pulsante "❓ Come si gioca" nell'header.
- [x] **📝 Corretto il testo Nuzlocke fuorviante** (3.3): non descrive più "1 solo tentativo di cattura per tappa" come regola esclusiva Hardcore — nessuna modalità offre mai un secondo tentativo sullo stesso incontro, quindi il testo era fuorviante.
- [x] **♿ Accessibilità da tastiera per tutti i modali** (4.3): nuovo hook `useModalA11y` (`PokedexModal`, `BoxModal`, `HallOfFameModal`, `ScoreCardModal`, `OnboardingModal`) — chiusura con `Esc`, `role="dialog"` / `aria-modal="true"`, focus intrappolato dentro il modale (Tab non esce più verso la pagina sotto), focus spostato automaticamente all'apertura.
- [x] **📱 Breakpoint mobile aggiuntivi** (4.1): nuovo `@media (max-width: 480px)` — padding ridotto, `team-matrix-grid` da 3 a 2 colonne (il `TeamPanel` era il componente più denso), pulsanti header più compatti e con `flexWrap` corretto (prima potevano uscire dallo schermo su telefoni stretti invece di andare a capo). Verificato: nessun overflow orizzontale a 375px di larghezza.
- [x] **👆 Tooltip descrizioni raggiungibili anche su touch** (4.2): nuovo `TapTooltip.js` — tocca per aprire una bolla con la stessa descrizione dell'attributo `title` (invisibile su touch), tocca altrove per chiuderla. Applicato alle chip abilità (`TeamPanel.js`) e agli strumenti in battaglia (`BattleScene.js`, con una ⓘ separata dal pulsante d'uso per non consumare l'oggetto per sbaglio toccando per informarsi). Le descrizioni del Box/zaino erano già visibili come testo, non serviva intervenire lì.

---

## 🔮 Prossimi Sviluppi & Pianificazione Futura

### 🎯 Fase 1: Espansione dei Bivi & Eventi Dinamici (L'Esperienza dei Bivi - COMPLETATO)
- [x] **🧪 Laboratorio Fossili**: evento speciale per consegnare un fossile antico e far rianimare Pokémon rari (Omanyte, Kabuto, Aerodactyl, Lileep, Cranidos, Tyrunt).
- [x] **🤝 Scambio Allenatore (NPC Trade)**: incontra un Allenatore sul percorso disposto a scambiare un suo Pokémon raro per una specie specifica.
- [x] **🎰 Casinò Razzo & Sala Giochi**: minigioco facoltativo per scommettere le monete guadagnate e vincere Porygon, Master Ball, Monete extra o strumenti rari.
- [x] **🌾 Zona Safari**: zona di esplorazione speciale ad alto rischio/ricompensa dove usare Safari Ball per catturare Pokémon esotici.
- [x] **🛒 Mercante Ambulante & Pietre Evolutive**: negozio itinerante per acquistare pietre evolutive specifiche (Pietra Focaia, Idropietra, Pietra Lunare) o Caramelle Rare.
- [x] **☀️ Sistema Meteo sui Percorsi**: eventi atmosferici dinamici (Sole Intenso, Pioggia Battente, Tempesta di Sabbia, Nebbia) che potenziano o indeboliscono determinati tipi durante l'esplorazione.
- [x] **🎰 Modalità "Chaos Roulette" (Opzionale)**: opzione "Ruota del Destino" per far girare una roulette nei bivi invece di scegliere manualmente.

---

### 🍇 Fase 2: Espansione Contenuti — Generazione 9 (Paldea)
- [x] **Starter Gen 9**: inseriti *Sprigatito*, *Fuecoco*, e *Quaxly*.
- [x] **Capipalestra & Lega di Paldea**: le 8 palestre di Paldea, i Superquattro (Rika, Poppy, Larry, Hassel) ed il Campione Geeta / Nemi.
- [x] **Leggendari & Ultra-Bestie di Paldea**: incontri con *Koraidon*, *Miraidon* ed *I Quattro Tesori della Rovina* (Chien-Pao, Ting-Lu, Wo-Chien, Chi-Yu).

---

### 💎 Fase 3: Meccaniche di Lotta Avanzate & Strumenti
- [x] **💎 Terastallizzazione (Terastal)**: meccanica di Gen 9 per Terastallizzare un Pokémon in battaglia (+25% potenza del tipo Tera con pulsante dedicato e sincronizzazione potenza).
- [x] **🎒 Strumenti Tenuti (Held Items)**: integrazione strumenti passivi in battaglia (Assorbosfera +20%, Resti +10%, Stolascelta +15%, Baccamela).

---

### ⚔️ Fase 4: Modalità di Gioco & Sfide (Challenge Modes)
- [x] **💀 Nuzlocke Hardcore**: attivabile sulla schermata di selezione starter — morte permanente, badge visivo, moltiplicatore punteggio x1.5.
- [x] **📐 Bivi post-game dinamici**: pool di 20+ bivi speciali con shuffle deterministico basato su `postgameRound`.
- [x] **🎲 Randomizer Mode** *(riattivata in v8.6, vedi sopra)*
- [x] **🔥 Mono-Type Challenge** *(riattivata in v8.6, vedi sopra)*
- [ ] **⏱️ Speedrun / Choice Timer**: tracciamento del numero di decisioni effettuate e tempo trascorso.

---

## 🐛 TODOLIST / Fix — Storico (✅ Risolto in v8.6)

Randomizer Mode e Mono-Type Challenge erano **strutturalmente implementate** in `challengeEngine.js` ma disabilitate perché causavano un **loop di re-render infinito** in React.

### Causa del Bug (comune a Randomizer e Mono-Type)

Il filtro veniva applicato durante il render React chiamando `filterEncounterPoolByChallenge(pool, state)` direttamente nel render path. Questa funzione usava `Math.random()` internamente, producendo output diverso ad ogni chiamata → React rileva props cambiate → re-render → loop → freeze del browser.

### Fix applicato (v8.6)

Il filtraggio è stato spostato **a monte**, dentro `goTo()` in `src/hooks/useGameState.js`: ogni volta che un `patch` passato a `goTo()` contiene `pendingEncounterPool` (e non è un incontro leggendario, vedi sotto), viene filtrato lì una volta sola e il risultato salvato in `state.pendingEncounterPool`, stabile tra i render. Lo starter viene filtrato allo stesso modo in `SceneRouter.js` con `filterStartersByChallenge`. Il toggle UI è in `GenerationSelectScreen.js`.

Un secondo bug emerso applicando il fix: il filtro veniva applicato anche ai pool di incontro leggendario (un solo id) — se il leggendario rollato non era del tipo scelto, il fallback del filtro lo sostituiva con un Pokémon comune qualsiasi dello stesso tipo. Risolto escludendo esplicitamente `pendingEncounterIsLegendary: true` dal filtro.

---

### 🏆 Fase 5: Sistema di Achievement, Impostazioni & QoL
- [x] **⚙️ Pannello Impostazioni (Settings Modal) — Tasso di Shiny**: nuovo `SettingsModal.js` + `engine/settings.js` (persistenza globale in `localStorage`, stesso criterio del mute audio). 3 modalità — *Default* (1/500 normali, 1/20 leggendari), *Aumentato* (1/100 normali, 1/4 leggendari), *Disattivato* (mai Shiny) — applicate in `EncounterScene.js`.
  - [ ] Velocità del testo e animazioni delle battaglie — rimandato: oggi non esiste alcun sistema di animazione/rivelazione testo da rendere configurabile, andrebbe costruito da zero.
  - [ ] Selettore Lingua (Italiano, Inglese, Spagnolo) — rimandato: richiederebbe tradurre l'intero gioco, sforzo molto più grande di una singola voce di backlog.
- [x] **🎨 Temi Visivi Personalizzabili**: nuovo `engine/theme.js` + selettore nel pannello Impostazioni. 5 temi (Dark Classic/default, Dark Synthwave, Retro GameBoy Green, Classic Emerald, Cyberpunk), ognuno un blocco `:root[data-theme="X"]` in `styles.css` che sovrascrive gli stessi token già usati ovunque (`--bg`, `--panel`, `--accent`, `--text`...) — nessun componente sa quale tema è attivo. Applicato subito al click (feedback immediato) e persistito in `localStorage`, applicato prima del primo render in `main.js` per evitare un flash del tema di default.
- [x] **🏆 Medagliere Trofei & Achievement**: nuovo `AchievementsModal.js` + `engine/achievements.js` (persistenza globale in `localStorage`, come la Sala della Fama storica — traguardi sull'insieme delle run, non per singolo salvataggio), consultabile dal pulsante "🏆 Trofei" nell'header. 4 trofei implementati:
  - 🌟 *"Luccichio Epico"*: Cattura il tuo primo Pokémon Shiny — sbloccato in `usePokedexState.markCaught`.
  - 👑 *"Master Nuzlocke"*: Completa una regione in modalità Nuzlocke senza perdere l'intera squadra — sbloccato alla vittoria sul Campione con `state.isNuzlocke` attivo (se il team fosse stato spazzato del tutto, il run si sarebbe fermato prima a `nuzlockeGameOver`, quindi arrivarci implica non averlo mai perso).
  - 🌋 *"Monte Argento"*: Sconfiggi Rosso Leggendario nel post-game — sbloccato alla vittoria contro l'"Allenatore Leggendario Rosso" in `SceneRouter.js`.
  - 🌍 *"Gran Maestro dei Continenti"*: Conquista tutte e 9 le regioni in una sola run — sbloccato quando `completedGensCount` raggiunge `GENERATIONS.length` al termine dell'ultima regione della catena.
- [x] **🎨 Carte Bivio in Griglia 2x2 con Border Glow**: layout a griglia con sfondi sfumati a tema per le scelte di esplorazione e bordi illuminati al passaggio del mouse.
- [x] **📊 Indicatore Visivo Dinamico Probabilità di Vittoria**: `computeWinChance` (già esistente, prima calcolato solo dopo aver scelto la tattica) ora chiamato in anteprima per tutte e 3 le tattiche mentre sono ancora selezionabili — una barra colorata (rosso <40%, oro 40-65%, verde >65%) con percentuale sotto ciascuna card di `BattleScene.js`, così la scelta è informata invece che alla cieca.
- [x] **📱 Header Responsive Compatto Mobile**: gli 8 pulsanti dell'header (Audio, Home, Pokédex, Impostazioni, Trofei, Come si gioca, Sala della Fama, Punteggio) sono definiti una sola volta e renderizzati in due modi — fila completa da tablet in su, menu "☰ Menu" a tendina sotto i 640px (chiusura al click fuori o su una voce). Verificato: a 400px la fila piena resta nascosta, il toggle mostra tutti gli 8 elementi ed apre correttamente il modale scelto.
- [x] **🎉 Effetti Particellari (Sparkles & Confetti)**: nuovo `ParticleBurst.js`, puro CSS + emoji (nessuna dipendenza esterna). Brillantini ✨ su cattura Shiny (`EncounterScene.js`) ed evoluzioni (`EvolutionNotice.js`); coriandoli 🎉 sulle vittorie importanti tramite nuova prop `celebrateOnWin` di `BattleScene.js`, attivata sulla vittoria contro il Campione e sulla finale del Torneo dei Campioni (non su ogni battaglia).
  - 🐛 **Fix**: sulla schermata Evoluzione le particelle apparivano intrappolate in una riga orizzontale invece di cadere su tutto lo schermo. Causa: `.evo-overlay` ha `backdrop-filter: blur()`, che crea un nuovo "containing block" per i discendenti `position: fixed` (li confina al riquadro dell'antenato invece che al viewport — una delle sorprese meno note del CSS). Risolto renderizzando `ParticleBurst` via `createPortal` direttamente su `document.body` (aggiunto `react-dom` all'import map in `index.html`, prima mappato solo `react-dom/client`), immune a qualsiasi filter/transform di un antenato indipendentemente da dove viene montato nell'albero React.
- [x] **⚡ Display Potenza Complessiva della Squadra**: indicatore visivo "⚡ Potenza Squadra: XXX" nel pannello laterale per confrontarla immediatamente con quella degli avversari.
- [x] **📐 Layout Griglia 2x3 Perfetto per il Team Panel**: struttura CSS Grid a 3 colonne per mantenere la squadra ordinata e bilanciata.
- [x] **📥 Gestione Flessibile del Box (Deposita / Ritira)**: possibilità di ritirare Pokémon dal Box se la squadra ha slot liberi (< 6) o depositare membri senza obbligo di scambio 1:1.
- [x] **↕️ Quick-Swap Posizione Pokémon**: pulsanti ◄ / ► sulle schede della squadra per riordinare la posizione dei Pokémon con un solo click.

---

### 📡 Fase 6: Integrazione Avanzata PokéAPI & Caching Locale (Offline-First)
- [x] **🔊 Versi Audio Ufficiali dei Pokémon (`cries`)**: `usePokemon.js` ora espone anche `data.cry` (URL `.ogg` da `data.cries.latest`, già incluso nella risposta di `/pokemon/{id}`). Nuova `playPokemonCry(url)` in `soundEngine.js` — usa l'elemento `<audio>` nativo, non l'oscillatore Web Audio dei jingle sintetizzati esistenti in quel file, perché qui è un file audio reale. Riprodotto alla rivelazione dell'incontro selvatico (`EncounterScene.js`) e alla nuova forma di un'evoluzione accettata (`EvolutionNotice.js`) — non ripetuto anche sulla cattura per evitare di far sentire due volte lo stesso verso a pochi secondi di distanza. Verificato con una richiesta di rete reale verso `PokeAPI/cries` (status 206, streaming audio).
  - 🐛 **Fix**: dopo questa modifica un giocatore ha riscontrato `SyntaxError: ... does not provide an export named 'playPokemonCry'`. Causa: `server.mjs` non mandava header anti-cache, quindi il browser aveva ancora in cache la vecchia versione di `soundEngine.js` mentre `EvolutionNotice.js` (modificato più di recente) veniva ricaricato fresco — importava un export non presente nella versione cache. Il file su disco era corretto fin dall'inizio. Risolto aggiungendo `Cache-Control: no-cache, no-store, must-revalidate` a tutte le risposte statiche di `server.mjs`, così una modifica al codice non resta mai invisibile dietro la cache del browser durante lo sviluppo.
- [x] **📖 Voci del Pokédex e Descrizioni in Italiano (`/pokemon-species`)**: nuovo `hooks/usePokemonSpecies.js` (stesso pattern cache-in-memoria di `usePokemon.js`) + pannello dettaglio in `PokedexModal.js`, apribile cliccando una specie già scoperta (griglia o lista). Mostra sprite, tipi, "genus" (es. "Pokémon Seme") e la descrizione Pokédex ufficiale in italiano più recente tra quelle disponibili per la specie. Caricata su richiesta per la sola specie selezionata, non per tutte le 1025 in una volta, per non intasare PokeAPI. Verificato con dati reali da PokeAPI (Bulbasaur, Charmander).
- [x] **🎒 Sprite Pixel Ufficiali degli Strumenti & Bacche (`/item`)**: nuovo `hooks/useItemSprite.js` (cache in memoria, stesso pattern di `usePokemon.js`) + componente `ItemIcon.js`, usato in PokéMart (`PokeCenterScene.js`), Zaino (`TeamPanel.js`) e strumenti in battaglia (`BattleScene.js`). Mappa `ITEM_POKEAPI_SLUGS` in `data/items.js` collega ~16 nomi italiani agli slug ufficiali PokeAPI (es. "Super Pozione" → `super-potion`) — solo per gli strumenti di cui sono ragionevolmente sicuro corrispondano a un oggetto reale dei giochi. Diversi nomi in questo gioco sono invenzioni di sapore senza equivalente ufficiale certo (es. "Biscotto Lavarone", "Galletta di Yantar", "Pietra Metallica"): per quelli niente slug, `ItemIcon` ripiega sull'emoji esistente invece di rischiare uno sprite sbagliato/fuorviante. Verificato: 5/5 sprite reali nel PokéMart, fallback emoji corretto per gli strumenti non mappati.
- [x] **🔮 Sprite Reali per Megaevoluzione/Gigamax (`/pokemon-species` → `varieties`)**: nuovo `hooks/useMegaSprite.js` (stesso pattern cache-in-memoria, due livelli: lista varietà per specie + sprite per varietà). Quando Mega/Gigamax è attivo in battaglia, ogni scheda squadra in `TeamPanel.js` interroga `varieties` della specie e cerca una forma con suffisso `-mega-x`, `-mega-y`, `-mega` o `-gmax` (in quest'ordine di priorità, così Charizard/Mewtwo mostrano X/Y invece di una scelta arbitraria); se trovata, sostituisce lo sprite normale con quello ufficiale della forma e aggiunge bordo viola + 🔮. Interrogato solo mentre Mega è attivo, per non sprecare chiamate a riposo. Specie senza alcuna variante Mega/Gigamax restano con lo sprite normale (il bonus +30% potenza si applica comunque a tutta la squadra, invariato). Verificato dal vivo: Charizard mostra Mega X, e — correttamente, non un bug — anche Pikachu mostra uno sprite alternativo perché possiede davvero una forma Gigamax ufficiale su PokeAPI (`pikachu-gmax`, introdotta in Spada/Scudo).
  - ⏸️ **Deferred**: supporto completo alle varianti regionali (Alola/Galar/Hisui/Paldea) NON incluso in questo step — è uno scope nettamente più ampio (specie diverse con Pokédex/stat/movepool propri, non solo uno sprite alternativo). Scelta esplicita dell'utente: "Solo sprite Mega/Gigamax per ora".
- [x] **⚡ Dettaglio Mosse & Icone Tipi (`/type` & `/move`)**: nuovo `data/movePicker.js` (funzione pura `pickSignatureMove`, testabile in isolamento) sceglie la "mossa firma" di una specie dall'array `moves` grezzo di `/pokemon/{id}` — la mossa imparata per livello (level-up) al livello più alto, criterio semplice che in pratica seleziona quasi sempre la mossa più iconica imparata naturalmente (es. Charizard → Flare Blitz lv.66, non una mossa base lv.1). `usePokemon.js` la espone come `data.signatureMoveRef` ({name, url}). Nuovo `hooks/useSignatureMove.js` fa il fetch dei dettagli reali (`/move/{id}`: potenza, precisione, tipo, nome italiano se disponibile) solo su richiesta, con cache in memoria condivisa per mossa. `PokemonChip` (`PokemonSprite.js`) accetta ora un prop `showMove` (default `false`, per non aggiungere fetch extra ovunque il chip è già usato, es. anteprima Box) che aggiunge l'icona del tipo primario e, al tap (`TapTooltip`), la mossa firma con potenza/precisione — attivato sulla sola squadra avversaria in `BattleScene.js`, coerente con lo scope "in battaglia" di questa voce. Le icone dei tipi restano gli emoji esistenti (`TYPE_ICONS` in `data/types.js`, già usati per medaglie/badge) invece di sprite `/type` scaricati da PokeAPI: nessun beneficio reale nel sostituire un set completo e già funzionante di 18 icone con richieste di rete aggiuntive — aggiunta solo una variante `getTypeIconFromSlug()` indicizzata sullo slug inglese di PokeAPI (`data.types` restituisce "fire"/"water", non "Fuoco"/"Acqua"). Verificato dal vivo: chip avversario Geodude in battaglia mostra 🏔️, al tap "Sdoppiatore — Potenza 120 / Precisione 100%" (Double-Edge, dati reali corretti). 2 nuovi test unitari (20/20 passano).
- [x] **📊 Base Stats Ufficiali (`/pokemon`)**: `usePokemon.js` espone ora anche `data.stats` (6 statistiche base) e `data.bst` (somma). Nuovo `hooks/useTeamStats.js` recupera il BST di tutta la squadra (riusando la stessa cache di `usePokemon.js`, nessuna richiesta doppia) e lo passa a `computeTeamPower(team, statsById)` in `battleLogic.js`. Scelta di design deliberata sul *come* integrarlo, per non dover ricalibrare i ~200 valori `opponentPower` hand-tuned in `generations.js`/`championsTournament.js`: ogni specie riceve un moltiplicatore di potenza `computeStatMultiplier(bst)` clampato 0.7×–1.4× in base allo scostamento del suo BST dalla media di riferimento (`AVERAGE_BST = 430`, media approssimativa tra tutte le ~1000 specie) — una squadra di specie "nella media" produce la stessa potenza di prima (calibrazione esistente intatta), mentre leggendari/pseudo-leggendari (es. Mewtwo, BST 680) pesano di più e specie notoriamente deboli (es. Magikarp, BST 200) pesano di meno. Dato mancante/non ancora caricato → moltiplicatore neutro 1.0, per non far "saltare" la potenza mostrata durante il fetch. Usato sia in `TeamPanel.js` (badge Potenza Squadra + BST nel tooltip di ogni scheda) sia in `BattleScene.js` (stesso input a `computeWinChance`, quindi ora incide realmente sull'esito della battaglia). Aggiunte anche 6 barre statistiche + Totale nel pannello di dettaglio del Pokédex (`PokedexModal.js`) per rendere il dato visibile, non solo un moltiplicatore nascosto. Verificato dal vivo: squadra Mewtwo+Magikarp stesso livello → potenza 88 contro 84 di una squadra "nella media" (calcolo motore e badge in-game combaciano esattamente); barre statistiche di Magikarp nel Pokédex corrispondono ai valori reali PokeAPI (PS 20/Atk 10/Def 55/SpA 15/SpD 20/Vel 80 = 200). 2 nuovi test unitari (18/18 passano).
- [x] **🎭 Nature dei Pokémon**: nuovo `data/natures.js` — 14 Nature reali (nomi italiani ufficiali: Decisa/Modesta/Timida/Gioviale/Impavida = +10% Potenza, Audace/Mite/Cauta/Fiacca/Placida = -10%, Ferma/Docile/Seria/Bizzarra = neutre, esistono anche nei giochi reali) integrate in `computeTeamPower` (`engine/battleLogic.js`), 4 nuovi test. Scelta deliberata di **non** usare `/nature` di PokeAPI: quell'endpoint descrive solo le 25 Nature in astratto (quale statistica alzano/abbassano), non l'assegnazione a un Pokémon specifico — nei giochi reali la Natura è casuale per singolo esemplare catturato, un dato che PokeAPI non può fornire in alcun modo. Assegnazione deterministica sul resto della divisione dell'id specie (stesso approccio di riempimento già usato in `data/abilities.js` per le specie non mappate esplicitamente, con lo stesso trade-off dichiarato: stabile per id anziché casuale per singolo esemplare). Visibile nel tooltip di ogni Pokémon nel pannello squadra (`TeamPanel.js`).
- [x] **♂️ Genere (`/pokemon-species`)**: `usePokemonSpecies.js` espone ora anche `genderRate` (`data.gender_rate` reale: -1 senza genere, 0 sempre maschio, 8 sempre femmina, 1-7 popolazione mista). Nuovo `data/gender.js` (`getGenderSymbol`, testato) — per le specie a popolazione mista il simbolo è deterministico sul resto della divisione dell'id (stesso trade-off di `data/natures.js`/`data/abilities.js`: nei giochi reali è casuale per esemplare, un dato che nessuna API può fornire per un individuo specifico). Simbolo ♂/♀ colorato mostrato accanto al nome di ogni Pokémon in squadra (`TeamPanel.js`) e nel tooltip. **Differenze visive di forma** (Pikachu coda a cuore ♀️, Pyroar ♂️/♀️) deliberatamente escluse da questo step: riguardano una manciata di specie specifiche con sprite alternativi dedicati, scope a parte rispetto al semplice badge di genere — stesso tipo di confine già tracciato per le varianti regionali Mega/Gigamax.
- [x] **🌌 Condizioni di Evoluzione Avanzate — Eevee Giorno/Notte**: nuova `resolveEeveeEvolution()` in `data/evolutions.js` (testata, `now` iniettabile) — Eevee (id 133) evolve in Espeon di giorno (6:00-17:59) o Umbreon di notte in base all'ora reale del sistema, unico caso speciale gestito direttamente in `checkEvolution()` invece di passare per la soglia di livello fissa usata da tutte le altre specie. Sostituisce il precedente comportamento semplificato (Eevee → sempre Flareon a lv36). Scope deliberatamente limitato a questo singolo caso, esplicitamente nominato nella voce originale del ROADMAP: **Felicità** e **Pietre Evolutive come vincolo reale** (non solo bonus di potenza in battaglia) restano fuori — introdurle davvero richiederebbe un contatore di amicizia persistente e un gating dell'evoluzione dietro al consumo di un oggetto specifico dallo zaino, un cambiamento strutturale al flusso di level-up che contraddirebbe la scelta di design già dichiarata in cima al file (`evolutions.js`: "condizioni speciali mappate a un livello-soglia equivalente per mantenere il gameplay narrativo") — da valutare separatamente se si vuole davvero rivedere quella decisione.
- [x] **📖 Numerazione Pokédex Regionale (`/pokedex`)**: nuovo `hooks/useRegionalDex.js` (`useRegionalDexNumber`, cache dell'intero Pokédex regionale per slug — molto più efficiente che una richiesta per specie). Ogni generazione in `data/generations.js` ha ora un campo `regionalDex` con lo slug PokeAPI verificato dal vivo (`kanto`, `updated-johto`, `updated-hoenn`, `extended-sinnoh`, `updated-unova`, `updated-alola`, `galar`, `paldea`) — Kalos è l'unica eccezione: PokeAPI non ha un Pokédex Kalos unificato (nei giochi reali è diviso in 3 sotto-Pokédex Centro/Costa/Montagna), quindi `regionalDex` è un array di 3 slug controllati in ordine. Numero regionale mostrato nel pannello dettaglio del Pokédex (`PokedexModal.js`) accanto al numero Nazionale, solo quando si gioca con una regione attiva.
- [x] **🚀 Architettura Caching Locale & Offline-First**: nuovo `sw.js` (Service Worker) registrato da `main.js`, con `CacheStorage`. Strategia **runtime**, non un elenco statico di file da precaricare: questo progetto non usa un bundler (decine di moduli ES separati, vedi `index.html`) — un manifest scritto a mano si romperebbe silenziosamente ad ogni file nuovo dimenticato. Ogni richiesta osservata finisce in cache al volo: dopo una prima visita online, tutto ciò che è stato caricato resta disponibile offline (comportamento standard per un Service Worker). Due strategie distinte: (1) PokeAPI/sprite/CDN esm.sh → *cache-first*, zero chiamate di rete ripetute e caricamenti istantanei anche online; (2) file statici dell'app (stesso dominio) → *network-first con fallback alla cache*, per non reintrodurre la classe di bug già risolta in v8.6 (`server.mjs` manda `Cache-Control: no-cache` proprio per evitare che una modifica durante lo sviluppo resti mascherata da una versione cachata) — la cache del Service Worker interviene solo quando la rete non risponde davvero (offline). **Scelta deliberata**: nessuno script `scripts/fetch-pokeapi-assets.js` di pre-fetch periodico — il Service Worker già ottiene runtime entrambi gli obiettivi dichiarati nella voce originale (azzerare le chiamate ripetute, caricamenti istantanei) in modo più robusto di uno snapshot JSON statico che andrebbe ri-generato a mano e duplicherebbe i dati su due livelli di cache diversi. **Non verificabile con `npm test`**: i Service Worker richiedono API browser (`self`, `caches`, evento `fetch`) assenti nel runtime di test Node — verificato solo staticamente (`node --check`) e che il server serva `sw.js` con lo status/Content-Type corretti; la registrazione effettiva e il comportamento offline richiedono un controllo manuale in DevTools (scheda Application → Service Workers, e Network → Offline).

---

### 🧬 Fase 7: Approfondimenti Mega/Oggetti & Meccaniche di Battaglia (idee da valutare, confronto con Pokemon Roulette)
- [x] **🔮 Megaevoluzione vincolata alla squadra reale**: scelta esplicita dell'utente tra le opzioni proposte — (A) nascondi il bottone se nessuno in squadra è davvero Mega/Gigamax-capace, invece di (B) scalare il bonus; fonte dati: PokeAPI dal vivo (stessa già usata per lo sprite in `useMegaSprite.js`) invece di una tabella statica scritta a mano, per zero rischio di lista incompleta/sbagliata su un elenco di ~80 specie. Nuovo `useTeamMegaCapability(team)` in `hooks/useMegaSprite.js`: riusa la stessa cache/logica (`fetchVarieties`, `FORM_SUFFIXES`) di `useMegaSprite` ma interrogata per **tutta la squadra e sempre**, non solo per un singolo Pokémon quando Mega è già attivo — necessario per sapere se mostrare il bottone *prima* che il giocatore lo prema. `BattleScene.js` nasconde il bottone Mega (`hasMegaCapableMember`) e mostra una nota discreta quando non disponibile, sostituita da niente finché il controllo asincrono non è concluso (`ready`) per evitare un falso negativo momentaneo durante il caricamento. Bonus e testo del bottone invariati (+30% flat quando disponibile) — solo la *visibilità* è cambiata, non la matematica. Nessun impatto sulla Terastallizzazione (nessuna restrizione di specie nei giochi reali, corretto lasciarla sempre disponibile). Non testabile con `npm test` (hook React con `fetch`/DOM, fuori dal runtime `node --test` di questo progetto, coerente con l'assenza di test sugli altri hook React) — 188/188 test esistenti invariati, verificato che il server serva i file modificati (200).
- [ ] **🎒 Strumenti tenuti per singolo Pokémon**: oggi i bonus da strumento (`items.js`) sono applicati come bonus di squadra piatto. Da valutare l'assegnazione di uno strumento tenuto a un singolo membro della squadra (più fedele ai giochi ufficiali) invece che a tutta la squadra insieme.
- [ ] **🍇 Bacche con effetti situazionali**: oggetti che curano una % di PS o rimuovono uno stato invece di dare solo +Potenza fissa, per differenziarle dalle pozioni.
- [ ] **🎁 Oggetti tematici per bivio**: pool di ricompense specifiche per certi bivi (es. la Zona Safari droppa i suoi oggetti propri) invece di attingere sempre allo stesso pool generico del PokéMart.
- [ ] **⚔️ Battaglie doppie / sinergia di tipo in squadra**: bonus di potenza per combinazioni di tipo complementari tra i 6 membri, oggi la potenza è pura somma senza sinergie.
- [ ] **🥊 Rivale ricorrente con squadra in evoluzione**: oggi il Rivale è un singolo scontro a metà percorso; da valutare farlo ricomparire più volte lungo la run con una squadra che cresce insieme alla tua.
- [ ] **🎯 Sistema di mosse reali con potenza/precisione in battaglia**: estendere il lavoro già fatto in `useSignatureMove.js`/`movePicker.js` (oggi mostra solo la mossa firma come info) verso un vero calcolo dell'esito di battaglia basato su mosse e tipi reali, invece delle "tattiche narrative" attuali (idea di lungo periodo già citata in `README.md`).

---

### 🩺 Fase 8: Bug & Miglioramenti da Segnalazione Utente (Backlog — 23/08/2026)

**Quick win (banale/medio, basso rischio):**
- [x] **💾 Più slot di salvataggio**: `DEFAULT_SLOTS` in `src/engine/saveGame.js:7` allargato da `[1,2,3]` a `[1,2,3,4,5]`; testo in `ResumeScreen.js` aggiornato. La UI era già generata dinamicamente dagli slot, nessun'altra modifica necessaria.
- [x] **🏷️ Tipo di partita nella card dello slot**: nuova `getModeBadges()` in `ResumeScreen.js` legge `isNuzlocke`/`isRandomizer`/`monoType` dallo stato salvato e li mostra come riga "Modalità" in ogni `SlotCard` (Standard / 💀 Nuzlocke / 🎲 Randomizer / Mono-Tipo).
- [x] **🎒 Raggruppare oggetti duplicati con quantità (es. "Pozione x12")**: nuova `groupItemsByName()` condivisa in `data/items.js`, usata sia nello Zaino (`TeamPanel.js`) sia nel selettore "Usa uno strumento prima dello scontro" (`BattleScene.js`) — la struttura dati sottostante (array con duplicati, consumo per indice) resta invariata, il raggruppamento è solo in fase di rendering.
- [x] **🏆 Master Ball garantita a fine Lega**: `addItem("Master Ball")` aggiunto nel blocco vittoria contro il Campione in `LeagueSceneContainer.js`.
- [x] **🛒 Mercante Ambulante: dare davvero una scelta**: nuovo `components/MerchantScene.js` (vetrina con prezzo e bottone "Compra" per oggetto, stesso pattern di `PokeCenterScene.js`) al posto dell'oggetto casuale gratuito. Il bivio ora apre la vetrina invece di assegnare subito un item; aggiunte anche descrizioni/slug PokeAPI mancanti per le pietre evolutive in `data/items.js`.

**Bug di correttezza (priorità alta):**
- [x] **🔥 Fix filtro Mono-Type Challenge da Johto in poi**: `getPokemonType()` (`src/data/types.js`) non ricade più sul fallback finto `id % 8` per gli id non presenti in `POKEMON_TYPES` — restituisce `null`. I 4 punti che la chiamano gestiscono già `null` in modo sicuro: il confronto `=== monoType` in `challengeEngine.js` lo esclude correttamente (invece di farlo passare per errore), mentre `computeTypeEffectiveness`/`computeWeatherEffect` trattano "nessun match" come neutro. Effetto pratico: un pool di incontro non mappato (es. tier Johto) non lascia più passare id di tipo sbagliato — ripiega sul lookup deterministico già esistente (`getMatchingTypePokemon`) che pesca solo tra i Pokémon davvero mappati con quel tipo. Scelta deliberata rispetto all'alternativa "completare `POKEMON_TYPES` con tutti gli id usati nei pool di `generations.js`": quella richiederebbe mappare a mano ~150-200 id aggiuntivi dalla memoria, con un rischio concreto di introdurre dati silenziosamente sbagliati (la stessa classe di bug che ha causato il problema originale) — la correttezza è garantita subito, la varietà tematica regionale nei pool mono-tipo resta un miglioramento facoltativo separato. Nuova `tests/challengeEngine.test.js` (5 test, incluso un test di regressione diretto sul bug con un pool Johto reale) + `npm test` ora a 40/40.

**Deciso e implementato:**
- [x] **🎰 Casinò Razzo con vera puntata**: nuovo `engine/casinoLogic.js` (funzione pura `rollCasinoOutcome`, testata) + `components/CasinoScene.js`. Il giocatore sceglie 5💰/10💰/20💰 prima del roll (bottone disabilitato se saldo insufficiente); probabilità invariate (25% jackpot / 40% vincita media / 35% piccola perdita), il payout scala linearmente sulla puntata (jackpot = puntata×3, vincita media = puntata×0.5, perdita = -puntata). Sostituisce il vecchio roll automatico gratuito sia nella variante pre-postgame che in quella postgame in `ExploreSceneContainer.js`.
- [x] **☀️ Meteo differenziato per tipo + feedback visibile**: nuovo `data/weather.js` (4 condizioni — Sole Intenso/Pioggia Battente/Tempesta di Sabbia/Nebbia Fitta, ciascuna con tipi bonus/malus propri) + `engine/weatherLogic.js` (`computeWeatherEffect`, testato). Il bivio "micro-Clima" ora estrae una condizione reale e la salva in `state.activeWeather`; si applica come moltiplicatore aggiuntivo (±10%/-8%) alla prossima battaglia ovunque essa sia (palestra/rivale/boss/Alto Comando/Campione/torneo/allenatore — tutti i 7 punti che renderizzano `BattleScene`), con un badge visibile sia in `BattleScene.js` (esito applicato) sia nel pannello squadra persistente (`TeamPanel.js`, mentre è in attesa), poi si consuma automaticamente alla risoluzione della battaglia.
- [x] **💎 Vero Tipo Tera per la Terastallizzazione**: nuova `computeTeraEffect` in `engine/typeMatchup.js` (testata). All'attivazione viene estratto un tipo Tera casuale tra i 18; il bonus non è più un flat +25% ma scala in base al type-matchup contro l'avversario (`TYPE_SUPER_EFFECTIVE`, la stessa tabella già usata per il vantaggio di tipo standard): +35% se il tipo Tera è Super Efficace contro l'avversario, +25% caso base neutro, +10% se è l'avversario ad avere il vantaggio. Messaggio esplicito nel badge di battaglia con l'esito reale. Il pannello squadra persistente (`TeamPanel.js`) non conosce l'avversario corrente quindi mostra il caso base +25% come approssimazione (documentato in un commento).
- [x] **🩹 Senso meccanico a "Cura la Squadra" (usura dopo sconfitta)**: nuovo campo persistente `state.teamFatigued` + `computeFatigueMultiplier` in `engine/battleLogic.js` (testato). `handleNuzlockeLoss()` in `useGameState.js` — già l'unico punto centralizzato chiamato ad ogni sconfitta in tutti i tipi di battaglia (palestra/rivale/boss/Alto Comando/Campione/torneo) — ora imposta `teamFatigued: true` incondizionatamente (non solo in Nuzlocke), con malus -10% Potenza Squadra nella battaglia successiva finché non si passa dal Centro Pokémon. "Cura la Squadra" azzera il flag (messaggio e bottone aggiornati per riflettere lo stato). Badge visibile sia in `BattleScene.js` sia nel pannello squadra persistente. Scope deliberatamente limitato: non cumulativo su sconfitte multiple consecutive (resta un singolo flag booleano, come deciso), e la sconfitta contro un Allenatore casuale sul percorso (`trainerBattle`) non la attiva, coerente con il fatto che quella battaglia non ha mai avuto conseguenze nemmeno in Nuzlocke.

---

### 🏗️ Fase 9: Architettura & Tooling (confronto con Pokemon Roulette)
Spunti emersi confrontando struttura/tooling reali di [zeroxm/pokemon-roulette](https://github.com/zeroxm/pokemon-roulette) (Angular 21 + TypeScript, cartelle per feature, test Jasmine colocati, `ngx-translate`) con l'impostazione attuale di questo progetto (React via CDN senza build, JS non tipizzato, cartelle per tipo, test centralizzati in `tests/`). Non urgenti alla scala attuale, ma da tenere d'occhio se il progetto continua a crescere:
- [ ] **🔷 Valutare migrazione (anche parziale) a TypeScript**: lo state di gioco (`useGameState.js` e derivati) è cresciuto parecchio dal refactoring di v8.5 — un typo in una chiave dello state oggi si scopre solo a runtime/test, con tipi si scoprirebbe a compile time. Da valutare anche solo JSDoc + `checkJs` come passo intermedio meno invasivo di una migrazione TS completa.
- [ ] **📂 Riorganizzazione cartelle per-feature invece che per-tipo**: oggi si separa per tipo (`components/`, `engine/`, `hooks/`, `data/`); se le feature continuano ad aumentare (vedi Fase 7), valutare un raggruppamento per funzionalità (component+logica+dati della stessa feature vicini) come fa l'originale con `wheel/`, `pokedex/`, `trainer-team/`.
- [ ] **🧪 Test colocati vicino al codice**: oggi tutti i test vivono in `tests/`, lontani dai file che testano; valutare di avvicinarli (es. `battleLogic.test.js` accanto a `battleLogic.js`) per rendere più immediato capire cosa ha/non ha coverage.
- [ ] **🌍 Infrastruttura i18n per l'estrazione stringhe**: base tecnica (dizionario centralizzato invece di stringhe italiane hardcoded nei componenti) come prerequisito per lo "Selettore Lingua" già segnato come rimandato in Fase 5 — oggi ogni stringa è inline nel JSX/`React.createElement`, quindi anche solo l'inglese richiederebbe toccare praticamente ogni componente.

---

### 🧪 Fase 10: Testing & CI più solidi
Spunto nato dal bug reale di `isPostgame` (Fase 8 sopra, risolto in `useGameState.js:206`): un `8` hardcoded invece di `GENERATIONS.length` faceva scattare il post-game appena raggiunta Paldea, saltando le sue 8 palestre. Nessun test esistente poteva scoprirlo, perché quella logica vive dentro un hook React non testabile in isolamento e `scripts/simulate-flow.mjs` reimplementa la macchina a stati a mano invece di richiamare il codice vero. Da qui, un piano per rendere la rete di test capace di intercettare da sola questa classe di bug.

**Prioritarie:**
- [x] **🧩 Estrarre la logica di transizione da `useGameState.js` in un modulo puro**: nuovo `src/engine/gameStateTransitions.js` (nessuna dipendenza da React) con `computeIsPostgame`, `computeDifficultyMultiplier`/`computeScaledPower`, `resolveAfterGymBattle`, `resolveNextGeneration`, `resolvePostgameExplore` (rng iniettabile, stesso pattern di `casinoLogic.js`) e `resolveNuzlockeLoss` — stesso approccio già usato per `explorePicker.js`. `useGameState.js` diventa un insieme di thin wrapper che chiamano i resolver puri e poi applicano `goTo`/`unlockAchievement`/`setState`; **firma pubblica invariata** per tutte le funzioni esportate dall'hook, quindi nessun altro file (`SceneRouter.js`, `GymBattleSceneContainer.js`, `LeagueSceneContainer.js`, `ExploreSceneContainer.js`) ha dovuto cambiare. `MAX_LEVEL`/`LEGENDARY_CHANCE` spostate nel nuovo modulo (costanti di bilanciamento pure, non hook-specific); `MAX_LEVEL` continua ad essere ri-esportata da `useGameState.js` per l'unico import esterno esistente (`ExploreSceneContainer.js`). Nuova `tests/gameStateTransitions.test.js` (22 test) copre tutti i resolver, incluso il regression test esplicitamente citato da questa fase — `computeIsPostgame` con `completedGensCount: 8` su Paldea (ultima regione) deve restituire `false` (il vecchio bug era un `8` hardcoded invece di `GENERATIONS.length`, che oggi vale 9). Come bonus quasi a costo zero, `scripts/simulate-flow.mjs` ora chiama la vera `resolveAfterGymBattle()` per la sequenza palestre→rivale→boss invece di reimplementarla a mano (anticipa parte del punto "🔁" più sotto) — la parte Alto Comando/Campione resta simulata a mano, dato che quella progressione vive in `LeagueSceneContainer.js` e non in `useGameState.js`, quindi non rientra in "estrarre da `useGameState.js`". Verificato con `npm test` (81/81), `node scripts/simulate-flow.mjs` (completa senza errori per tutte e 9 le regioni, output identico a prima) e un diff a mano riga per riga di `useGameState.js` per confermare che ogni branch/patch sia rimasto comportamentalmente identico, non solo che i test passino.
- [x] **📊 Test di integrità dati su `generations.js`/`championsTournament.js`**: nuova `tests/dataIntegrity.test.js` (9 test) che scorre tutte e 9 le regioni e verifica automaticamente forma e coerenza — 8 capipalestra con potenza strettamente crescente, `teamIds` validi (array non vuoti di interi positivi) su capipalestra/Alto Comando/Campione/Rivale/Boss del team nemico, Alto Comando presente con potenza crescente, Campione con potenza superiore all'ultimo membro dell'Alto Comando, tipo medaglia risolvibile e riconosciuto via `getBadgeType`/`TYPE_LIST`, starter/leggendari/Pokédex regionale definiti, `afterGymIndex` di Rivale/Boss nel range delle palestre, più 5 round del Torneo dei Campioni (`championsTournament.js`) sequenziali con potenza crescente.
  - **🐛 Trovato un bug di dati reale al primo giro**: la prima palestra di Unova aveva `title: "Capopalestra di tipo Elementale"` — "Elementale" non è mai stato un tipo Pokémon reale (placeholder per la Palestra di Striaton dei giochi originali, dove il capopalestra affrontato dipende dallo starter scelto tra Cilan/Erba, Chili/Fuoco, Cress/Acqua — qui semplificata a un solo capopalestra fisso). Il tipo non essendo in `TYPE_LIST`, `getBadgeType()` restituiva un valore non riconosciuto e l'icona di quella medaglia ricadeva silenziosamente sul fallback generico invece di un'icona di tipo vera, unica su tutte le ~72 palestre del gioco. `teamIds: [506, 512]` (Lillipup + Simisage, l'evoluzione Erba di Pansage) corrispondeva già di fatto alla squadra di Cilan/Erba dei giochi originali — cambiato solo il tipo dichiarato in "Capopalestra di tipo Erba" per farlo combaciare con la squadra già presente, nessun'altra modifica.
  - Verificato: 90/90 test (81 + 9 nuovi), `node scripts/simulate-flow.mjs` invariato (completa senza errori per tutte e 9 le regioni), server ha servito i file modificati (200).
- [x] **⚙️ CI su GitHub Actions**: nuovo `.github/workflows/tests.yml` — su push a `main` e su ogni pull request, checkout + `actions/setup-node@v4` (Node 22, stessa versione già in uso in locale) + `npm test` + `node scripts/simulate-flow.mjs`. Nessuno step di `npm install`/`npm ci`: zero dipendenze npm nel progetto (React via CDN, nessun `package-lock.json`), coerente con la filosofia "zero build" già dichiarata in `package.json`. **Nota**: questo repository non ha oggi un remote GitHub configurato (`git remote -v` vuoto) — il workflow è pronto ma resta inerte finché non viene pushato su un repository GitHub reale.
- [x] **🔁 `simulate-flow.mjs` deve pilotare la macchina a stati vera, non una sua copia**: chiuso del tutto. Nuova `resolveAfterEliteBattle(state, generation)` in `engine/gameStateTransitions.js` (stesso pattern di `resolveAfterGymBattle`) estrae la decisione "prossimo membro dell'Alto Comando o sfida al Campione", finora inline dentro `LeagueSceneContainer.js`. Sia `LeagueSceneContainer.js` sia `scripts/simulate-flow.mjs` ora la richiamano invece di reimplementarla — l'intera sequenza palestre→rivale→boss→Alto Comando→Campione simulata dallo script passa finalmente per il codice vero, non più una copia a mano. 3 nuovi test in `gameStateTransitions.test.js`. Verificato: `simulate-flow.mjs` completa senza errori per tutte e 9 le regioni, output identico a prima.
- [x] **📝 Disciplina "ogni bug fix porta con sé un test di regressione"**: il fix di `isPostgame` ora ha il suo test dedicato (`computeIsPostgame` con `completedGensCount: 8` su Paldea → `false`, in `tests/gameStateTransitions.test.js`). Regola adottata per il seguito: ogni fix da qui in avanti porta il proprio test.

**Estensioni successive (opzionali, più pesanti):**
- [x] **📈 Coverage report nativo**: nuovo script `npm run test:coverage` (`node --test --experimental-test-coverage tests/*.test.js`) in `package.json`, zero dipendenze nuove. Copertura complessiva già alta (98.15% linee, 85.71% branch, 90.82% funzioni) grazie ai test aggiunti in questa stessa fase — punti più scoperti individuati per un giro futuro: `saveSanitizer.js` (100% linee ma solo 19.35% branch/20% funzioni: quasi tutti i rami di fallback per dati corrotti restano mai esercitati, solo il caso "tutto valido" e "tutto invalido" sono testati oggi), `challengeEngine.js` (80.21% linee, 60.87% branch), `scoreLogic.js` (86.78% linee, 35.71% branch), `typeMatchup.js` (86.78% linee, 70% branch), `generations.js` (66.67% funzioni: il ramo "nessuna prossima regione" di `getNextGeneration` e il fallback di `getExplorationTier` per generazione nulla non sono coperti). Nessun test nuovo scritto in questo giro — solo lo strumento di misura, come da scope della voce.
- [x] **🛡️ Colmato il gap di `saveSanitizer.js` (19.35% branch)**: nuova `tests/saveSanitizer.test.js` (15 test) copre ogni singolo ramo di fallback per campo — non solo il caso "tutto valido"/"tutto invalido insieme" già testato in `engine.test.js`. Branch coverage del modulo passato dal 19.35% al 96.72%, coverage complessivo del progetto dall'85.71% al 91.62%.
  - **🐛 Bug reale trovato scrivendo questi test**: `completedGensCount`, `postgameRound`, `tournamentRound` e `choicesCount` non avevano la guardia `!isNaN(...)` che invece proteggeva già `gymIndex`/`eliteIndex`/`coins` — un salvataggio corrotto con uno di questi campi a `NaN` attraversava il sanitizzatore indenne (`Math.max(0, NaN)` restituisce `NaN`, non `0`), con `NaN` che si sarebbe poi propagato in aritmetica a valle (`postgameRound + 1`, il seed di `explorePicker.js` basato su `choicesCount`, e proprio i confronti `count >= GENERATIONS.length` al cuore della Fase 10). Corretto aggiungendo la stessa guardia già presente sui campi analoghi, nessun altro cambiamento.
- [x] **🗄️ Chiuso il gap "localStorage mai esercitato nei test" su `scoreLogic.js`/`achievements.js`/`hallOfFame.js`**: i tre moduli usano `localStorage` (record personale, trofei, Sala della Fama), assente nel runtime `node --test` — `achievements.js`/`hallOfFame.js` non comparivano proprio nel coverage report (nessun test li importava, quindi Node non li strumentava nemmeno), `scoreLogic.js` era fermo al 35.71% branch. Nuovo `tests/helpers/localStorageStub.js` (fuori dal glob `tests/*.test.js`, non è un file di test): uno stub in-memory di `getItem`/`setItem`/`removeItem`/`clear`, più `withThrowingLocalStorage(fn)` per simulare il caso "storage non disponibile" (es. modalità privata) ed esercitare anche i rami `catch` finora mai raggiunti. Nuovi `tests/scoreLogic.test.js` (7 test, `getHighScore`/`saveHighScore` — `computeVictoryScore` resta coperta da `engine.test.js`), `tests/achievements.test.js` (9 test) e `tests/hallOfFame.test.js` (5 test). Copertura complessiva del progetto salita al 98.83% linee / **92.61% branch** / 94.33% funzioni (da 98.25/91.62/92.34). - [x] **📈 Chiusi anche i due gap lasciati aperti**: `computeVictoryScore` (soglie dei Gradi S/A/B/C, moltiplicatore Nuzlocke, bonus generazioni, Shiny) aveva un solo caso testato in `engine.test.js` — 9 nuovi test in `scoreLogic.test.js` coprono stato nullo/vuoto, il confine esatto di ciascun Grado (8000/15000/25000, incluso il "nessuna soglia raggiunta" mai esercitato prima), `isNuzlocke` disattivo e i bonus isolati. Branch coverage di `scoreLogic.js`: 59.09% → **96.43%**. Nuovo `tests/saveGame.test.js` (22 test) copre l'intero modulo di salvataggio multi-slot — mai testato prima, non compariva nemmeno nel coverage report: round-trip salva/carica, filtro dei campi transienti (`SKIP_FIELDS`), JSON corrotto, versione di salvataggio incompatibile, retro-compatibilità col vecchio `pcq_save_v1` (solo per lo slot 1), `loadAllSlots`/`hasSave`/`deleteSave`, import/export JSON, Pokédex storico incluso lo sblocco di `pokedexComplete` esattamente alla 151ª specie. Branch coverage di `saveGame.js`: da invisibile (0, nessun test lo importava) a **98.21%**. Copertura complessiva finale: **98.95% linee / 94.73% branch / 95.24% funzioni** (da 98.83/92.61/94.33 di inizio giornata).
- [x] **🏁 Chiusi tutti i rimanenti gap di coverage del progetto**: nuovi `tests/typeMatchup.test.js` (guardia parametri mancanti, ramo "svantaggio di tipo", fallback "neutro", match per sottostringa nei titoli regionali — `computeTypeEffectiveness` aveva un solo caso testato che usciva sempre dalla funzione al primo `return`), `tests/generations.test.js` (`getGeneration`/`getExplorationTier`/`getNextGeneration`, mai testate direttamente: id sconosciuto, generazione nulla, ultima regione, indice oltre l'ultimo tier), `tests/types.test.js` (`getTypeIcon`/`getTypeIconFromSlug`, mai chiamate da nessun test). Estesi `tests/natures.test.js` (fascia Natura sconosciuta, id assente) e `tests/engine.test.js` (`rollBattle` mai chiamata; `computeWinChance` testata solo con tattica Bilanciata e nessuna abilità — aggiunte Aggressiva/Difensiva e le 3 abilità passive Prepotenza/Pressione/Acceleratore). Estese anche le suite di `challengeEngine.test.js` (Randomizer Mode, priorità su Mono-Type, fallback quando nessun tipo combacia da nessuna parte) e `saveSanitizer.test.js` (bug nei test stessi scoperto durante questo giro: `box`/`items` erano testati solo col ramo "non-array", il filtro sugli array veniva eseguito solo per `team`/`badges` — corretto testando anche `box`/`items` con array reali contenenti voci da scartare).
  - **📊 Risultato finale**: **99.98% linee / 97.03% branch / 98.90% funzioni** su tutto il progetto (praticamente ogni file sorgente ora al 100% linee, la maggior parte anche al 100% branch/funzioni). 182 test totali (da 90 a inizio Fase 10). Il branch residuo non chiuso (`challengeEngine.js` 85.71%, `typeMatchup.js` 84%) corrisponde a combinazioni non naturalmente costruibili con i dati reali del gioco (es. un "tipo" con esattamente 1-2 Pokémon mappati in `POKEMON_TYPES`, quando ogni tipo reale ne ha 6+) — non forzato con dati finti per non introdurre test artificiosi scollegati da scenari di gioco plausibili.
  - Verificato: 182/182 test, `node --check` su tutti i file toccati, `simulate-flow.mjs` invariato, server ha servito i file (200).
- [x] **🖥️ Test di rendering/routing su `SceneRouter.js`**: implementata l'alternativa leggera già indicata qui, invece di introdurre jsdom/`@testing-library/react`. Nuovo `engine/sceneRouting.js` con `resolveSceneForPhase(phase)`, una tabella pura fase→chiave-scena costruita enumerando ogni stringa di fase realmente usata nel codice (`goTo("...")` e `phase: "..."` letterali, verificati via grep). `SceneRouter.js` riscritto per confrontare `sceneKey === "..."` invece di `state.phase === "..."` in ogni condizione di routing (compresi i gruppi multi-fase: `explore`/`postgameExplore`, `gymBattle`/`rivalBattle`/`villainBossBattle`, ecc.) — l'unico `state.phase === "legendaryEncounter"` rimasto è logica di business interna al blocco `encounter` (distingue leggendario/normale), non una decisione di routing, lasciato intatto. Nuova `tests/sceneRouting.test.js`: verifica che ogni fase nota mappi alla scena attesa, che una fase sconosciuta ricada sempre sull'hub di emergenza invece di far crashare l'app, e che i gruppi multi-fase restino raggruppati sulla stessa scena. Nessun rendering coinvolto, solo la decisione.

---

### 🎮 Fase 11: UX & Bilanciamento — Feedback Utente (Backlog — 23/08/2026)

- [x] **🎰 Casinò: rivelare il Pokémon vinto al jackpot**: `playCasino()` (`ExploreSceneContainer.js`) ora imposta `outcome.pokemonId` (Porygon `137` o Porygon2 `233`, già deciso 50/50 internamente) sull'oggetto esito restituito, invece di tenerlo solo per sé. `CasinoScene.js` salva `pokemonId` nello stato locale dell'esito e, al jackpot, mostra sprite+nome reali tramite `PokemonChip` (`PokemonSprite.js`, stesso componente già usato altrove — nessun nuovo modo di caricare i dati) sotto il messaggio, aggiornato per introdurre la card invece di dire genericamente "un Pokémon raro".
- [x] **🎾 Indicatore "già catturato" nell'incontro**: `EncounterScene.js` riceve ora `pokedexRun` da `SceneRouter.js` (già presente in `state`, nessun nuovo dato) e calcola `alreadyCaught` confrontando l'id del Pokémon incontrato con `pokedexRun[wildId]?.caught` — letto dallo stato *prima* che questo stesso incontro venga marcato come visto da `onSeen`, quindi riflette correttamente solo catture di run precedenti, non quella in corso. `PokemonPreview` (`PokemonSprite.js`) accetta il nuovo prop `alreadyCaught` e mostra una pill "⚪ Già catturato" sopra lo sprite, stessa struttura visiva già usata per i badge Shiny/leggendario (nuova classe `.caught-badge` in `styles.css`). 54/54 test invariati; verificato che il server serva correttamente i file modificati (nessuna verifica visiva in browser in questa sessione — nessun tool headless-browser disponibile nell'ambiente).
- [x] **🏆 Più achievement facili + notifica a schermo**: aggiunti 4 nuovi trofei early-game a `engine/achievements.js` — 🎯 *"Primo Passo da Allenatore"* (prima cattura, `usePokedexState.markCaught`), 🦋 *"Metamorfosi"* (prima evoluzione accettata, `App.js:handleDismissEvolutions`), 🏅 *"Prima Medaglia"* (primo Capopalestra battuto, `resolveBattleWin(badge)` in `useGameState.js` solo quando `badge` è valorizzato — la vittoria sul Campione richiama la stessa funzione ma nel normale flusso di gioco arriva sempre dopo le 8 palestre, quindi il trofeo risulta già sbloccato a quel punto), 🟣 *"Cattura Garantita"* (prima Master Ball ottenuta, agganciato centralmente in `addItem()` così copre tutte le fonti — PokéMart, ricompensa Boss/Campione, jackpot Casinò — con un solo punto di codice). Nuovo sistema di notifica disaccoppiato: `onAchievementUnlocked()`/listener interni a `achievements.js` (nessuna callback da passare in giro tra i vari hook/componenti che chiamano `unlockAchievement()`), nuovo `AchievementToast.js` montato una sola volta in `App.js` (via `createPortal` su `document.body`, stesso pattern di `ParticleBurst.js`) che mostra in coda ogni sblocco con icona+titolo per 4 secondi, indipendentemente da quale scena sia attiva. Nuove classi `.achievement-toast*` in `styles.css`. Aggiunti anche 3 trofei di follow-up su richiesta esplicita dell'utente — 🥇 *"Maestro delle Palestre"* (tutti gli 8 Capipalestra di una regione, `advanceAfterGymBattle()` in `useGameState.js` quando `nextGymIndex >= generation.gymLeaders.length`), 🏆 *"Campione della Lega"* (primo Campione battuto, `LeagueSceneContainer.js`) e 🐉 *"Collezionista di Leggende"* (almeno un leggendario catturato in ciascuna delle 9 regioni **nella stessa run** — `caughtLegendaries` non si azzera passando da una regione alla successiva, verificato leggendo `startPostgameExplore()` che lo confronta contro i leggendari dell'ultima regione visitata; il controllo in `SceneRouter.js` verifica con `GENERATIONS.every(...)` che ogni regione abbia almeno un id in comune con `caughtLegendaries` appena aggiornato). "Tutte le leghe" (tutte le 9 regioni completate) non è un trofeo nuovo: coincide con `grandMaster`, già esistente da prima di questa voce. Per "completare il Pokédex", scelta esplicita dell'utente tra le opzioni proposte: 📕 *"Maestro del Pokédex"* — soglia storica cumulativa a 151 specie diverse catturate su `pcq_pokedex_historic` (across tutte le run sul browser, non nella singola run), non il Pokédex regionale (scartato: con le tabelle di incontro attuali, sparse, probabilmente irraggiungibile in una singola run — vedi voce Fase 11 sull'ampliamento pool). Implementato centralmente in `updateHistoricPokedex()` (`saveGame.js`, già l'unico punto chiamato sia da una cattura normale che da un'evoluzione accettata) invece che nei singoli call-site, contando le entry `caught: true` dello storico ad ogni aggiornamento.
- [x] **🌎 36 achievement per-regione (4 categorie × 9 regioni)**: su richiesta esplicita dell'utente — trofeo per "tutte le palestre", "Campione", "un leggendario" e "il boss del team nemico" **per ciascuna regione**, non solo genericamente come le voci già esistenti. Generati automaticamente in `engine/achievements.js` a partire da `GENERATIONS` (`data/generations.js`) invece di 36 oggetti scritti a mano — se una nuova regione viene aggiunta al gioco, i suoi 4 trofei compaiono da soli. Nuovo helper `regionAchievementId(categoryKey, generationId)` usato sia per generarli sia dai 4 punti che li sbloccano (tutti riusano una `generation`/`gym`/`champion`/`boss` già disponibile localmente, nessun nuovo dato aggiunto): `advanceAfterGymBattle()` (`useGameState.js`, tutte le palestre), `LeagueSceneContainer.js` (Campione), `SceneRouter.js` (cattura leggendario, stesso punto già toccato per `legendaryCollector`), `GymBattleSceneContainer.js` (boss del team nemico, descrizione presa dal `villainBoss.title` reale della regione per un tocco di sapore, es. "Capo Team Rocket — Giovanni"). **Medagliere (`AchievementsModal.js`) riscritto** per restare leggibile con 44 trofei totali (8 generali + 36 per-regione): sezione generale invariata in cima, poi 9 sezioni per regione collassabili (chiuse di default, contatore `sbloccati/4` nell'intestazione), scelta esplicita dell'utente tra le opzioni proposte. Non verificabile con `npm test` (il modulo achievements richiede `localStorage`, assente nel runtime di test Node, coerente con l'assenza di test già presente su questo file) — verificato staticamente (`node --check` su tutti i file toccati) e che il server serva correttamente i file modificati/nuovi (200); nessuna verifica visiva in browser in questa sessione (nessun tool headless-browser disponibile nell'ambiente).
- [x] **🎒 Diversificare gli effetti degli strumenti oltre al +Potenza flat**: scoperto un gap concreto tra dati e comportamento durante l'analisi — Assorbosfera/Stolascelta/Baccamela avevano già descrizioni "diverse" in `data/items.js` (+20%, +15%, annulla malus di tipo) ma `BattleScene.js` le ignorava completamente: `handleUseItem` faceva string-matching solo su "Iper"/"Super"/"Pozione"/"Pietra"/"Rimedio", quindi tutte e 3 ricadevano sul bonus fisso di default (+10), mai sull'effetto reale promesso. Nuovo modulo puro `engine/itemEffects.js` (`computeItemEffect(itemName)`, testato in `tests/itemEffects.test.js`) restituisce un effetto tipizzato (`flat`/`percent`/`neutralizeType`) invece di un numero — **Assorbosfera** ora dà davvero +20% moltiplicativo, **Stolascelta** +15% moltiplicativo, **Baccamela** annulla per davvero lo svantaggio di tipo (stesso meccanismo già usato per l'abilità Levitazione in `BattleScene.js`, ora condiviso). Un secondo bug scoperto nello stesso giro: **Stolascelta e Baccamela non erano mai ottenibili in gioco** — nessuno shop/ricompensa le assegnava mai, dati morti fin dalla loro introduzione (Fase 3). Aggiunte al mercante post-game (`SceneRouter.js`).
  - **🎯 Pallina Esca riconvertita in oggetto di cattura reale**: prima dava anch'essa +10 Potenza flat in battaglia nonostante la descrizione "attirare e pacificare i Pokémon" non c'entrasse nulla con un bonus da battaglia. Ora è usabile **solo nell'incontro selvatico** (`EncounterScene.js`, nuovo bottone dedicato, consumata all'attivazione indipendentemente dall'esito) e dà +8% probabilità di cattura con Poké Ball/Cibo (dimezzato sui leggendari, stesso trattamento già riservato a Leggiadria — nuovo 5° parametro `hasBallLure` in `computeCaptureChance()`, `battleLogic.js`). Rimossa dalla lista oggetti utilizzabili in `BattleScene.js` (non ha più senso lì). Ora acquistabile anche nel PokéMart principale (`PokeCenterScene.js`, 2💰).
  - **🐛 Fix in `SceneRouter.js`**: consumare Master Ball e Pallina Esca nello stesso `onResolved` richiede rimuovere due indici dallo stesso array `items` — rimuoverli in ordine crescente avrebbe sfasato il secondo indice dopo la prima `splice`. Risolto ordinandoli decrescente prima di consumarli.
  - 6 nuovi test (`tests/itemEffects.test.js` + 2 aggiunti a `tests/engine.test.js`), 59/59 totali. Nessuna modifica alle Pozioni/Pietre esistenti (restano flat, coerenti con le proprie descrizioni) — scope limitato ai 4 oggetti dove descrizione e comportamento erano davvero disallineati, non un redesign di tutti i ~25 oggetti.
- [x] **🌿 Ampliare i pool di incontro poveri/ripetitivi**: tutti e 108 gli array (`grass`/`fishing`/`cave`/`grass2` × 3 tier × 9 regioni) in `data/generations.js` portati da 3-4 a **7-8 id** ciascuno (media 7.08, +765 id totali), sempre **aggiungendo in coda** senza mai riordinare o rimuovere id esistenti — l'unico punto del codice che legge un indice specifico (`tier.grass[0]`/`tier.cave[0]` in `ExploreSceneContainer.js`, per scegliere l'avversario "rappresentativo" di un Allenatore/Torneo) continua quindi a restituire esattamente lo stesso Pokémon di prima. Specie scelte a mano per generazione nativa di ciascuna regione (Kanto 1-151, Johto 152-251, ecc.), riusando dove sensato lo stesso pattern già presente nei dati originali di specie "generiche ricorrenti" tra regioni (Zubat/Geodude/Magikarp/Onix/Seadra/Octillery/Luvdisc comparivano già in pool di regioni diverse dalla propria prima di questa modifica). Verificato con uno script Node ad-hoc su tutti i 108 array: zero id duplicati all'interno dello stesso array, zero valori non numerici; un controllo di plausibilità sui range dex per regione ha segnalato 6 id "fuori range" — confermato via `git diff` che erano **tutti già presenti prima di questa modifica** (nessun errore introdotto). Nessuna logica toccata (`getExplorationTier`, `filterEncounterPoolByChallenge`, `explorePicker.js` restano invariati) — 54/54 test invariati.
- [x] **📐 Riequilibrare il numero di bivi mostrati nel post-game**: `pickTopIds(postgameSpecialPool.map(...), postgameSeed, 4)` in `ExploreSceneContainer.js` portato da 4 a **6** opzioni speciali a round, ora coerente con `pickWeightedIds(allSpecialOptions, explSeed, 6)` già usato pre-postgame tra le palestre. Bivi post-game ora 6 speciali + Erba fissa + Ruota del Destino = 8 scelte visibili (erano 6), vicino alle 9 del pre-postgame (che ha anche un "Allenamento" fisso assente nel pool post-game). Nessun'altra logica toccata: `pickTopIds` resta la stessa funzione pura già testata in `tests/explorePicker.test.js` — quei test invocano la funzione con un proprio `n=4` indipendente dal valore usato qui, quindi restano validi invariati (54/54 ancora passano).
- [x] **🔁 Anti-ripetizione anche sull'estrazione del Pokémon selvatico dentro una zona**: nuovo campo persistente `state.lastEncounterId` (`useGameState.js` + whitelist `saveSanitizer.js`, altrimenti andrebbe perso al primo salvataggio/ripresa — stessa classe di bug già presa a v8.6 per `nextGenId`). `EncounterScene.js` ora esclude `lastEncounterId` dal pool prima dell'estrazione casuale (`pickableIds`), ma solo se il pool ha altre opzioni disponibili — sui pool a un solo id (leggendari, fossili, scambio NPC...) il filtro ripiega subito sul pool intero, quindi resta innocuo lì. Scelta deliberata di **non** tracciare l'ultimo id per singola zona (grass/fishing/cave separatamente): i punti che generano `pendingEncounterPool` in `ExploreSceneContainer.js` sono **17**, molti con pool tematici fissi indipendenti dai tier regionali — tracciare una memoria per-zona avrebbe richiesto passare un identificativo di zona a ciascuno dei 17 call-site. Una singola memoria globale "ultimo Pokémon incontrato, qualunque zona" copre lo stesso bisogno dell'utente (mai rivedere subito lo stesso) con un solo punto di stato, a costo di essere leggermente più aggressiva (esclude l'id anche cambiando zona) — compromesso giudicato ragionevole visto che nessun giocatore si aspetta di rivedere lo stesso Pokémon nell'incontro immediatamente successivo, a prescindere dalla zona. L'id viene aggiornato in `SceneRouter.js` ad ogni risoluzione dell'incontro (`onResolved` ora riceve sempre `wildId`, anche quando la cattura fallisce o si sceglie "Ignora e prosegui" — prima quei due casi scartavano l'id). 54/54 test invariati.
- [ ] **✨ Piccolo bonus per catturare una specie mai vista nel Pokédex**: oggi nessuna differenza meccanica tra catturare un doppione e una nuova entry (solo `state.pokedexRun` viene aggiornato). Da valutare un piccolo bonus di catch-rate o XP quando l'id non è ancora `caught` in `pokedexRun`, per premiare meccanicamente il completamento del Pokédex e non solo esteticamente — reso più visibile una volta introdotto l'indicatore "già catturato" del punto sopra.

---

### 🩺 Fase 12: Post-Game — Bug da Segnalazione Utente

Tre problemi segnalati da un giocatore reale sull'esplorazione libera post-game, tutti confermati leggendo il codice prima di intervenire.

- [x] **🐛 I leggendari delle altre regioni non comparivano mai, solo le Ultra Bestie**: causa reale — il roll ambientale di un leggendario casuale (`resolvePostgameExplore` in `gameStateTransitions.js`) pescava solo da `generation.legendaries`, cioè la **singola regione fissa** in cui ci si trova (`state.generationId`, che in post-game non cambia mai). Una volta catturati tutti i leggendari di quella sola regione, `availableLegendaries.length` restava 0 per sempre e il roll non scattava più — lasciando la Fenditura Ultra-Varco (Ultra Bestie, un bivio a parte non soggetto a questo filtro) come unica fonte rimasta di incontri rari. Scelta esplicita dell'utente tra le opzioni proposte: pool unico con **tutti** i leggendari di **tutte e 9 le regioni** (`GENERATIONS.flatMap((gen) => gen.legendaries)`), non solo quelle visitate — firma di `resolvePostgameExplore` semplificata di conseguenza (non serve più passare `generation`). 2 nuovi test di regressione in `gameStateTransitions.test.js` (incluso uno che simula "ho già catturato tutti i leggendari di Kanto" e verifica che un leggendario di Johto resti comunque raggiungibile).
- [x] **🐛 Potenza avversari post-game senza tetto, a volte oltre i 1000 punti**: causa reale — due bivi post-game (`⚔️ Sfida un Allenatore di passaggio`, `🕵️ Incursione del Team Nemico` in `ExploreSceneContainer.js`) calcolavano la potenza come `35/100 + state.postgameRound * 5` **senza alcun limite**, mentre il livello dei Pokémon incontrati (`pgLevel`) era già correttamente limitato a `MAX_LEVEL` con `Math.min(...)` — un'incoerenza tra le due formule, non intenzionale (`postgameRound` avanza ad ogni singola azione in un ciclo esplicitamente "infinito", quindi dopo un centinaio di round la potenza superava facilmente i 1000 mentre una squadra reale resta comunque limitata dal livello massimo). Scelta esplicita dell'utente tra le opzioni proposte: la crescita si ferma nello stesso round in cui `pgLevel` raggiunge già `MAX_LEVEL` (nuovo `powerGrowthRound`, un `Math.min` sullo stesso `state.postgameRound` usato altrove), invece di un tetto assoluto arbitrario — nessun nuovo numero da bilanciare, i due sistemi restano sincronizzati.
- [x] **🌿 Pool tematici post-game troppo ristretti**: le zone a tema del post-game (Vulcano, Foresta Stregata, Vetta Innevata, Dojo, Parco Safari, Laboratorio Fossili, Scambio NPC, Uovo Misterioso) avevano pool fissi di 7-11 id ciascuna. Ampliati a 13-19 id ciascuno (+~90 id totali), aggiungendo specie autentiche delle generazioni meno rappresentate — in particolare Galar (i cui 4 ibridi fossili `Dracozolt`/`Arctozolt`/`Dracovish`/`Arctovish`, id 880-883, mancavano del tutto dal pool fossili) e Paldea. Scope dichiarato: un ampliamento sostanziale per più varietà verso il completamento del Pokédex nazionale, non l'elenco letterale delle 1025 specie in ogni singola zona (impraticabile e non è comunque il modo in cui funziona il post-game di nessun gioco Pokémon reale). Verificato con uno script Node ad-hoc: zero id duplicati all'interno di ciascun array.
- Verificato: 189/189 test, `node --check` su tutti i file toccati, `simulate-flow.mjs` invariato, server ha servito i file modificati (200).
